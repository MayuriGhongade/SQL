show databases;
use excelr2;

/* CONSTRAINTS: 
THESE ARE SET OF RULES GIVEN FOR COLUMN VALIDATION */

create table customer
(
CID INT PRIMARY KEY,
CNAME VARCHAR(50) NOT NULL,
ADDRESS VARCHAR(100) NOT NULL,
CPHNO BIGINT UNIQUE CHECK(LENGTH(CPHNO)=10),
PID int,
CITY VARCHAR(50) DEFAULT 'PUNE'
);

DESC CUSTOMER;

create table product
(
PID INT PRIMARY KEY,
PNAME VARCHAR(50) NOT NULL UNIQUE,
PRICE INT,
QUANTITY INT
);

desc product;

alter table customer
add constraint PID foreign key(PID) references PRODUCT(PID);

drop table product;

alter table customer 
add ORDERID int;

create table zomato
(
ORDERID INT PRIMARY KEY,
FOOD_NAME VARCHAR(100),
PRICE INT,
QUANTITY int
);


ALTER TABLE CUSTOMER 
ADD CONSTRAINT ORDERID FOREIGN KEY(ORDERID) REFERENCES ZOMATO(ORDERID);

DROP TABLE ZOMATO;

CREATE TABLE STUDENT
(
SID INT,
PLACEMENTID INT PRIMARY KEY,
STUDENT VARCHAR(50),
SPHNO CHAR(10) CHECK(LENGTH(SPHNO)=10) UNIQUE,
SADD VARCHAR (100) NOT NULL
);

CREATE TABLE PLACEMENT
(
PLACEMENTID INT,
CNAME VARCHAR(10) NOT NULL UNIQUE,
CADD VARCHAR(50),
CPHNO BIGINT PRIMARY KEY,
CONSTRAINT PLACEMENTID FOREIGN KEY (PLACEMENTID) references STUDENT(PLACEMENTID)
);

INSERT INTO ZOMATO VALUES(1,'POHA',10,1);
INSERT INTO PRODUCT VALUES(1,'LAPTOP',50000,1);
INSERT INTO CUSTOMER VALUES(1,'ARUN','MUMBAI',1234567890,1,DEFAULT,1);

SELECT * FROM CUSTOMER;
DESC product;


0






































































